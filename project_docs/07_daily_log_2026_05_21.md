# 07 Daily Log - 2026-05-21

## What Was Completed

今天完成了 Phase 1 的数据理解和第一张工作表构建。

### 1. Added Dataset Relationship Diagram

图片已保存为：

```text
project_docs/assets/olist_dataset_relationship_diagram.png
```

并加入：

```text
project_docs/02_olist_data_understanding.md
```

### 2. Documented Phase 1 Field Dictionary

新增：

```text
project_docs/05_phase1_field_dictionary.md
```

该文档逐张说明 Phase 1 使用的 6 张表：

```text
olist_order_items_dataset.csv
olist_orders_dataset.csv
olist_products_dataset.csv
product_category_name_translation.csv
olist_customers_dataset.csv
olist_sellers_dataset.csv
```

并说明每个字段：

```text
是否使用
字段含义
为什么有用
是否能用于报价
会产生哪些派生字段
```

### 3. Completed Data Audit

新增：

```text
project_docs/06_phase1_data_audit.md
```

关键结论：

```text
order_items rows: 112,650
unique exposure key: 112,650
orders with multiple item rows: 9,803
orders with multiple sellers: 1,278
max item rows in one order: 21
```

这证明不能直接把 `order_id` 当作唯一 exposure。

基础风险单位继续定义为：

```text
order_id + order_item_id + seller_id + product_id
```

### 4. Built Exposure Table Script

新增：

```text
src/build_exposure_table.py
```

脚本从 `olist_order_items_dataset.csv` 出发，连接：

```text
olist_orders_dataset.csv
olist_products_dataset.csv
product_category_name_translation.csv
olist_customers_dataset.csv
olist_sellers_dataset.csv
```

### 5. Generated Processed Outputs

输出：

```text
data/processed/exposure_table.csv
data/processed/exposure_table_summary.json
```

当前结果：

```text
rows: 112,650
columns: 29
unique exposure key: 112,650
unique order_id: 98,666
unique seller_id: 3,095
unique product_id: 32,951
orders with multiple exposures: 9,803
orders with multiple sellers: 1,278
max exposures per order: 21
```

## Important Findings

### Exposure Grain Is Valid

`order_id + order_item_id + seller_id + product_id` 在当前数据中唯一。

### Joins Are Reliable

核心连接关系健康：

```text
order_items -> orders: 100%
order_items -> products: 100%
order_items -> sellers: 100%
orders -> customers: 100%
products -> translation: 99.96%
```

### Missing Values Are Small

只有商品重量/尺寸相关字段存在极少缺失：

```text
product_weight_g: 0.016%
product_length_cm: 0.016%
product_height_cm: 0.016%
product_width_cm: 0.016%
product_volume_cm3: 0.016%
```

### Freight Ratio Has Outliers

运费占比存在明显高值：

```text
freight_to_price_ratio max: 26.235
```

后续建模前应考虑：

```text
winsorize
cap
log transform
bucket/binning
```

## Next Session Start Here

下一步做 exposure-level EDA。

建议新增脚本：

```text
src/eda_exposure_table.py
```

建议新增文档：

```text
project_docs/08_exposure_level_eda.md
```

下次优先回答这些问题：

```text
1. 各品类 exposure 数和运费分布如何？
2. 各路线 exposure 数和运费分布如何？
3. freight_value 和 freight_to_price_ratio 是否需要 cap？
4. 重量和体积是否有异常值？
5. 哪些字段需要 missing value handling？
6. 是否只保留 delivered 订单用于第一版理赔模拟？
```

## Current Project State

Phase 1 状态：

```text
data understanding: done
field dictionary: done
data audit: done
exposure table build: done
exposure-level EDA: done
synthetic claims: done
baseline pricing: done
GLM pricing: next
```

## Follow-Up Completed: Exposure-Level EDA

新增脚本：

```text
src/eda_exposure_table.py
```

新增文档：

```text
project_docs/08_exposure_level_eda.md
```

新增输出目录：

```text
data/processed/eda/
```

关键结论：

```text
delivered exposures: 110,197 / 112,650 = 97.82%
cross-state exposure share: 63.82%
freight_value P99: 84.52
freight_to_price_ratio P99: 1.549
freight_to_price_ratio max: 26.235
```

下一步：

```text
构建第一版 synthetic returns + claims layer。
第一版 claim simulation population 建议使用 delivered exposures only。
```

## Follow-Up Completed: Synthetic Claims Layer

新增脚本：

```text
src/build_synthetic_claims.py
```

新增文档：

```text
project_docs/09_synthetic_claims_design.md
project_docs/10_synthetic_claims_build.md
```

新增输出：

```text
data/processed/exposure_claims_synthetic.csv
data/processed/synthetic_claims_summary.json
```

关键结果：

```text
eligible exposures: 110,197
return requested count: 9,338
covered claim count: 8,485
covered claim frequency among eligible: 7.70%
total paid loss: 163,112.68
pure premium per eligible exposure: 1.48
```

下一步：

```text
构建 baseline pricing。
```

## Follow-Up Completed: Baseline Pricing

新增脚本：

```text
src/build_baseline_pricing.py
```

新增文档：

```text
project_docs/12_baseline_pricing_design.md
project_docs/13_baseline_pricing_build.md
```

新增输出：

```text
data/processed/pricing_baseline.csv
data/processed/pricing_baseline_summary.json
data/processed/pricing_by_category.csv
data/processed/pricing_by_route.csv
data/processed/pricing_by_cross_state.csv
data/processed/pricing_by_seller.csv
```

关键结果：

```text
target loss ratio: 60%
portfolio pure premium: 1.4802
portfolio commercial premium: 2.4670
total commercial premium: 271,854.47
expected loss ratio: 60.00%
```

当前状态：

```text
data understanding: done
field dictionary: done
data audit: done
exposure table build: done
exposure-level EDA: done
synthetic claims: done
baseline pricing: done
GLM pricing: next
```

## Follow-Up Completed: GLM Pricing

新增脚本：

```text
src/build_glm_pricing.py
```

新增文档：

```text
project_docs/14_glm_pricing_design.md
project_docs/15_glm_pricing_build.md
```

新增输出：

```text
data/processed/pricing_glm.csv
data/processed/glm_pricing_summary.json
data/processed/glm_frequency_coefficients.csv
data/processed/glm_severity_coefficients.csv
data/processed/glm_frequency_calibration.csv
data/processed/glm_severity_calibration.csv
data/processed/glm_pure_premium_calibration.csv
```

关键结果：

```text
frequency target: covered_claim_flag
severity target: net_loss, covered claims only
test frequency AUC: 0.560
test pure premium A/E: 1.003
GLM mean expected loss: 1.4958
GLM total commercial premium: 274,718.50
GLM expected loss ratio: 60.00%
```

当前状态：

```text
data understanding: done
field dictionary: done
data audit: done
exposure table build: done
exposure-level EDA: done
synthetic claims: done
baseline pricing: done
GLM pricing design: done
GLM pricing build: done
seller credibility: next
```
